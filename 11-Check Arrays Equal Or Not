class Solution{
    public static boolean check(long arr1[],long arr2[],int n){
      Arrays.sort(arr1);
      Arrays.sort(arr2);
      
      for(int i=0;i<n;i++)
        if(arr1[i]!=arr2[i])
          return false;
           
      return true;
    }
}
